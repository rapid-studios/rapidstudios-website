export const meta = {
  slug: "frontend-implementation",
  title: "Frontend implementation",
  kicker: "Design carried cleanly into production",
  summary: "Ship the approved direction in Next.js with reusable sections, motion tokens, and state coverage that survives launch.",
  deliverables: ["Next.js app shell", "Reusable components", "MDX content setup", "State and motion implementation"],
  outcomes: ["Faster launch", "Lower design drift", "Easier future editing"],
  featured: true
};

## What this covers

The implementation layer is treated as part of the design quality, not a handoff afterthought. The result should still feel premium once it becomes a real site with forms, loading states, and mobile constraints.
